'use strict';
// var lib = require('./components/components');
(function(scope){
  var polymer = scope.Polymer('five-star', {
        domReady: function () {
        },
        ready: function () {
        },
    });
    return polymer;
})(window);
